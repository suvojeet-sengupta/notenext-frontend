services:
  web:
    build:
      context: .
      args:
        # Backend URL is inlined at build time (NEXT_PUBLIC_*).
        NEXT_PUBLIC_API_BASE_URL: ${NEXT_PUBLIC_API_BASE_URL:-https://api-notenext.suvojeetsengupta.in}
    ports:
      - "3000:3000"
    restart: unless-stopped

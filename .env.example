# Backend API base URL. Inlined into the client bundle at BUILD time.
# The browser calls this directly, so the backend must allow CORS from this app.
NEXT_PUBLIC_API_BASE_URL=https://api-notenext.suvojeetsengupta.in
